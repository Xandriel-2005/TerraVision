# TerraVision IBVAP — AI Pipeline Configuration
# Each camera maps to a video file (or RTSP URL for live feeds)

cameras:
  - id: "cam-bop-01"
    name: "BOP Alpha - Main Gate"
    source: "./demo_videos/demo1.mp4"
    loop: true
    enabled: true

  - id: "cam-bop-02"
    name: "BOP Alpha - Perimeter North"
    source: "./demo_videos/demo2.mp4"
    loop: true
    enabled: true

  - id: "cam-bop-03"
    name: "BOP Bravo - Checkpoint"
    source: "./demo_videos/demo3.mp4"
    loop: true
    enabled: true

  - id: "cam-bop-04"
    name: "BOP Bravo - Fence Line"
    source: "./demo_videos/demo1.mp4"
    loop: true
    enabled: true

# Backend WebSocket URL
backend_ws: "ws://localhost:8000/ws/pipeline"

# YOLOv8 model — using nano (yolov8n) for CPU performance
model: "yolov8n.pt"

# Detection confidence threshold (0.0 - 1.0)
confidence: 0.4

# Inference device — using CPU since no CUDA GPU detected
device: "cpu"

# Target frames per second (lower for CPU)
target_fps: 8
